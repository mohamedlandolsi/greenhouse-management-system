apiVersion: v1
kind: Secret
metadata:
  name: db-secrets
  labels:
    app: greenhouse
type: Opaque
data:
  # IMPORTANT: Replace these with production-strength passwords
  # Base64 encoded - update with strong passwords before deploying
  # Example: echo -n 'your-strong-password' | base64
  postgres-env-password: REPLACE_WITH_PRODUCTION_PASSWORD_BASE64
  postgres-ctrl-password: REPLACE_WITH_PRODUCTION_PASSWORD_BASE64
